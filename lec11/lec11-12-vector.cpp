#include <iostream>
#include <vector>
using namespace std;

void f() {

	/*
	v.push_back(30);
	v.push_back(20);
	v.push_back(10);
	v.push_back(70);
	v.push_back(80);
	v.push_back(90);	
	*/
}

int main() {
	f();
	return 0;
}
