#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;


int main() {

	vector<int> v; 

	v.push_back(30);
	v.push_back(20);
	v.push_back(10);
	v.push_back(70);
	v.push_back(80);
	v.push_back(90);


	return 0;
}
